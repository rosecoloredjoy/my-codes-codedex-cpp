#include <iostream>

int main() {
  // Write code here
  std::cout << "Qual a sua temperatura agora?";
  int std::cin >> temp;

   if (temp >= 100) {
     std::cout << "You have a fever.";
   }
   else {
     std::cout << "You do not have a fever";
   }      
}
